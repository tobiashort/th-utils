```
Usage:
  columns [OPTIONS] [File]

Options:
  -n, --number <Number>  The number of columns (default: 4)
  -h, --help             Show this help message and exit

Positional arguments:
  File                   The file to be loaded otherwise Stdin is used

```

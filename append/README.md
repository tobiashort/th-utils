```
Reads from Stdin and appends each line with the given suffix.

Usage:
  append [OPTIONS] <Suffix>

Options:
  -h, --help  Show this help message and exit

Positional arguments:
  Suffix      the string that should be appended
```

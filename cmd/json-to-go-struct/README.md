```
Usage:
  json-to-go-struct [OPTIONS] [File]

Options:
  -h, --help  Show this help message and exit

Positional arguments:
  File        JSON file to convert. Reads from Stdin if not specified

```

```
Usage:
  substring [OPTIONS] --end <End> [String]

Required options:
  -e, --end <End>      End of the substring (exclusive).

Options:
  -s, --start <Start>  Start of the substring. (default: 0)
  -h, --help           Show this help message and exit

Positional arguments:
  String               The input string. Reads from Stdin if not specified.

```

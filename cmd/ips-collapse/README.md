```
Usage:
  ips-collapse [OPTIONS] [File]

Options:
  -h, --help  Show this help message and exit

Positional arguments:
  File        File with IP addresses or CIDRs separated by newlines. Reads from Stdin if not specified.

```

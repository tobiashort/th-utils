```
Usage:
  url-decode [OPTIONS] [String]

Options:
  -p, --path   Unescapes the string from inside a URL path segment
  -q, --query  Unescapes the string from inside a URL query
  -h, --help   Show this help message and exit

Positional arguments:
  String       The string to dencode. Reads from Stdin if not specified.

```

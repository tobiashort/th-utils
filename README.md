```
Usage:
  th-utils [OPTIONS]

Options:
  -p, --prefix <Prefix>          the prefix each binary will be given
  -i, --installdir <InstallDir>  install directory where tool are going to be installed
  -r, --readmes                  generates README.md for each tool
  -h, --help                     Show this help message and exit


```

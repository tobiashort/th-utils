```
Reads from Stdin and prints the maximum.

Usage:
  max [OPTIONS]

Options:
  -i, --ignore-na-n  Ignores not numeric values and treats them as 0.
  -h, --help         Show this help message and exit

```

```
Usage:
  th-count-files-by-extension [OPTIONS] [Path]

Options:
  -h, --help  Show this help message and exit

Positional arguments:
  Path        A file system directory path, or a file containg paths, or reads from Stdin if not specified

```

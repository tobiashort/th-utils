```
Usage:
  th-base64 [OPTIONS] [Text]

Options:
  -r, --raw     no padding
  -u, --url     url and file safe
  -d, --decode  decode otherwise encode
  -h, --help    Show this help message and exit

Positional arguments:
  Text          The string to encode/decode. Reads from stdin if not specified

```

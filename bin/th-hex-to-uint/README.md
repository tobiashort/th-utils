```
Usage:
  th-hex-to-uint [OPTIONS] [Hex]

Options:
  -h, --help  Show this help message and exit

Positional arguments:
  Hex         The hex value to convert. Reads from Stdin if not specified.

```

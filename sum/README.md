```
Reads from Stdin and counts together all numeric values

Usage:
  sum [OPTIONS]

Options:
  -i, --ignore-nan  Ignores not numeric values and treats them as 0.
  -h, --help        Show this help message and exit

```

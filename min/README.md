```
Reads from Stdin and prints the minimum.

Usage:
  min [OPTIONS]

Options:
  -i, --ignore-nan  Ignores not numeric values and treats them as 0.
  -h, --help        Show this help message and exit

```

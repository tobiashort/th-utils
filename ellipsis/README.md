```
This program reads string from stdin and cuts ist at the specified length minus three and adds three dots.

Usage:
  ellipsis [OPTIONS]

Options:
  -l, --length <Length>  Max length of the string including the three dots (default: 20)
  -h, --help             Show this help message and exit
```
